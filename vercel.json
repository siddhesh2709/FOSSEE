{
  "version": 2,
  "buildCommand": "cd frontend-web && npm install && npm run build",
  "outputDirectory": "frontend-web/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
